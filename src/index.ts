/**
 * @amlplugins/twilio-conversations
 *
 * Thin re-export of twilio (CommonJS `export =` SDK).
 *
 * Twilio Conversations — cross-channel chat (SMS, MMS, WhatsApp, chat) with participants and webhooks.
 */

import _sdk from "twilio";
export { _sdk as sdk };
export default _sdk;
